import React from "react";
import { Text, View } from "react-native";

// Trong video tutorial, mọi người thấy có dòng style={styles.container} nhưng code này thì ko có, vì vậy mọi người đừng lo lắng và đừng thêm nó vào phần <View> nhé,
// Nhưng lát nữa anh có thêm cái <Login> với <SignUp> thì mấy bạn thêm được nè :D

export default function App() {
  return (
    <View>
      <Text>Hello Cat </Text>
    </View>
  );
}
